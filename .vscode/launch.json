{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Chrome with Space Invaders",
            "file": "${workspaceFolder}/index.html",
            "webRoot": "${workspaceFolder}",
            "userDataDir": false
        }
    ]
}